"use client";
import Link from"next/link";import{ChevronLeft,ChevronRight,Clock3,MapPin}from"lucide-react";import{useMemo,useState}from"react";
const key=v=>{const d=new Date(v);return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`},monthKey=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`;
export function AgendaCalendar({items,month,onMonthChange,loading=false,full=false}){const[selected,setSelected]=useState(null),date=month||new Date(),grouped=useMemo(()=>items.reduce((a,x)=>{(a[key(x.at)]||=[]).push(x);return a},{}),[items]),first=new Date(date.getFullYear(),date.getMonth(),1).getDay(),total=new Date(date.getFullYear(),date.getMonth()+1,0).getDate(),cells=[...Array(first).fill(null),...Array.from({length:total},(_,i)=>i+1)],upcoming=selected?grouped[selected]||[]:items.filter(x=>new Date(x.at)>=new Date()).slice(0,full?12:6),move=n=>onMonthChange?.(new Date(date.getFullYear(),date.getMonth()+n,1));return <div className={`agenda-board${full?" full":""}`}><section className="agenda-upcoming"><div className="agenda-section-head"><div><span className="eyebrow">{selected?"Compromissos do dia":"Próximos compromissos"}</span><h3>{selected?new Date(`${selected}T12:00:00`).toLocaleDateString("pt-BR",{day:"2-digit",month:"long"}):"Sua agenda"}</h3></div><span className="badge">{upcoming.length}</span></div><div className="agenda-event-list">{upcoming.map(x=><Link href={x.href} className="agenda-event" key={x.id}><span className={`agenda-event-mark ${x.type}`}/><div><b>{x.title}</b><span><Clock3 size={13}/>{new Date(x.at).toLocaleString("pt-BR",{day:"2-digit",month:"short",hour:"2-digit",minute:"2-digit"})}</span>{x.project&&<span><MapPin size={13}/>{x.project}</span>}</div></Link>)}{!upcoming.length&&<div className="empty">Nenhum compromisso para exibir.</div>}</div></section><section className="agenda-month"><div className="agenda-month-head"><button className="icon-button" type="button" onClick={()=>move(-1)} aria-label="Mês anterior"><ChevronLeft/></button><div><span className="eyebrow">Agenda mensal</span><h3>{date.toLocaleDateString("pt-BR",{month:"long",year:"numeric"})}</h3></div><button className="icon-button" type="button" onClick={()=>move(1)} aria-label="Próximo mês"><ChevronRight/></button></div><div className="calendar-weekdays">{["Dom","Seg","Ter","Qua","Qui","Sex","Sáb"].map(x=><span key={x}>{x}</span>)}</div><div className="calendar-grid">{cells.map((day,i)=>{if(!day)return <span className="calendar-day empty-day" key={`e${i}`}/>;const k=monthKey(date)+`-${String(day).padStart(2,"0")}`,count=grouped[k]?.length||0;return <button type="button" key={day} className={`calendar-day${count?" has-items":""}${selected===k?" selected":""}`} onClick={()=>setSelected(selected===k?null:k)}><span>{day}</span>{count>0&&<b>{count}</b>}</button>})}</div>{loading&&<p className="meta">Atualizando agenda…</p>}</section></div>}

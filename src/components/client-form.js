export function ClientForm({action,client={},submitLabel="Salvar cliente",error}){return <form action={action} className="panel form-panel">{error&&<p className="error">{error}</p>}<div className="form-grid"><Field label="Nome *" name="name" defaultValue={client.name} required/><Field label="Razão social" name="legal_name" defaultValue={client.legal_name}/><Field label="E-mail" name="email" type="email" defaultValue={client.email}/><Field label="Telefone" name="phone" defaultValue={client.phone}/><Field label="WhatsApp" name="whatsapp" defaultValue={client.whatsapp}/><Field label="Origem" name="source" defaultValue={client.source}/><label className="field"><span>Status</span><select name="status" defaultValue={client.status||"active"}><option value="active">Ativo</option><option value="inactive">Inativo</option></select></label><label className="field full"><span>Observações</span><textarea name="notes" rows="6" defaultValue={client.notes||""}/></label></div><div className="form-actions"><button className="btn primary" type="submit">{submitLabel}</button></div></form>}
function Field({label,name,type="text",defaultValue="",required=false}){return <label className="field"><span>{label}</span><input name={name} type={type} defaultValue={defaultValue||""} required={required}/></label>}

"use client";import{useEffect,useState}from"react";import{AgendaCalendar}from"./agenda-calendar";const mk=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`;export function AgendaPageClient({projectSlug=""}){const[month,setMonth]=useState(()=>new Date()),[items,setItems]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);fetch(`/api/agenda-summary?month=${mk(month)}${projectSlug?`&projeto=${encodeURIComponent(projectSlug)}`:""}`).then(r=>r.json()).then(x=>setItems(x.items||[])).finally(()=>setLoading(false))},[month,projectSlug]);return <AgendaCalendar items={items} month={month} onMonthChange={setMonth} loading={loading} full/>}

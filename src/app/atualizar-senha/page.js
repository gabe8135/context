import{updatePassword}from"./actions";export default async function UpdatePassword({searchParams}){const q=await searchParams;return <main className="login-card"><div className="eyebrow">Nova senha</div><h1>Proteja seu acesso</h1>{q.erro&&<p className="error">{q.erro}</p>}<form action={updatePassword}><div className="field"><label>Nova senha</label><input name="password" type="password" minLength="8" required/></div><button className="btn primary">Salvar nova senha</button></form></main>}

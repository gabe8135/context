import Link from"next/link";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";export const dynamic="force-dynamic";
export default async function Meetings(){const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("meetings").select("id,title,scheduled_at,participants,summary,projects(name,slug)").eq("workspace_id",workspaceId).is("archived_at",null).order("scheduled_at",{ascending:false});if(error)throw error;return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Histórico</div><h1 className="page-title">Reuniões</h1></div><Link className="btn primary" href="/app/reunioes/nova">Nova reunião</Link></div><section className="panel data-panel"><table><thead><tr><th>Reunião</th><th>Projeto</th><th>Data</th></tr></thead><tbody>{data.map(m=><tr key={m.id}><td><Link href={`/app/reunioes/${m.id}`}><b>{m.title}</b></Link><div className="meta">{m.summary||m.participants}</div></td><td><Link href={`/app/projetos/${m.projects?.slug}`}>{m.projects?.name}</Link></td><td>{new Date(m.scheduled_at).toLocaleString("pt-BR")}</td></tr>)}</tbody></table></section></div></AppShell>}

import Link from"next/link";import{AppShell}from"@/components/app-shell";import{ClientForm}from"@/components/client-form";import{createClientAction}from"../actions";import{requireWorkspace}from"@/lib/auth-context";
export default async function NewClientPage({searchParams}){await requireWorkspace();const query=await searchParams;return <AppShell><div className="content narrow"><Link className="back-link" href="/app/clientes">← Clientes</Link><div className="eyebrow">Novo registro</div><h1 className="page-title">Criar cliente</h1><p className="subtitle">Comece com o essencial. Você poderá completar as informações depois.</p><ClientForm action={createClientAction} error={query.erro}/></div></AppShell>}

import Link from"next/link";import{notFound}from"next/navigation";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";import{archiveClientAction}from"../actions";
export default async function ClientDetail({params,searchParams}){const{id}=await params;const query=await searchParams;const{supabase,workspaceId}=await requireWorkspace();const{data:c,error}=await supabase.from("clients").select("*,projects(id,name,slug,status,progress)").eq("id",id).eq("workspace_id",workspaceId).is("archived_at",null).single();if(error?.code==="PGRST116")notFound();if(error)throw error;const archive=archiveClientAction.bind(null,id);return <AppShell><div className="content"><Link className="back-link" href="/app/clientes">← Clientes</Link><div className="project-head"><div><div className="eyebrow">Cliente · {c.status==="active"?"Ativo":"Inativo"}</div><h1 className="page-title">{c.name}</h1><p className="subtitle">{c.legal_name||"Contexto central do relacionamento"}</p></div><div className="actions"><Link className="btn primary" href={`/app/clientes/${id}/editar`}>Editar cliente</Link><form action={archive}><button className="btn" type="submit">Arquivar</button></form></div></div>{query.sucesso&&<p className="success-note">{query.sucesso}</p>}<div className="dashboard-grid" style={{marginTop:25}}><section className="panel"><header className="panel-head"><div className="panel-title">Contato</div></header><div className="panel-body"><Info label="E-mail" value={c.email}/><Info label="Telefone" value={c.phone}/><Info label="WhatsApp" value={c.whatsapp}/><Info label="Origem" value={c.source}/></div></section><section className="panel"><header className="panel-head"><div className="panel-title">Projetos</div><span className="badge">{c.projects.length}</span></header><div className="panel-body">{c.projects.length?c.projects.map(p=><Link className="item" style={{textDecoration:"none",color:"inherit"}} href={`/app/projetos/${p.slug}`} key={p.id}><div className="item-main"><div className="item-title">{p.name}</div><div className="meta">{p.status} · {p.progress}%</div></div></Link>):<p className="meta">Nenhum projeto vinculado.</p>}</div></section></div>{c.notes&&<section className="panel" style={{marginTop:20}}><header className="panel-head"><div className="panel-title">Observações</div></header><div className="panel-body"><p>{c.notes}</p></div></section>}</div></AppShell>}
function Info({label,value}){return <div className="item"><div className="item-main"><div className="meta">{label}</div><div className="item-title">{value||"Não informado"}</div></div></div>}

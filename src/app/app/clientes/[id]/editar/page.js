import Link from"next/link";import{notFound}from"next/navigation";import{AppShell}from"@/components/app-shell";import{ClientForm}from"@/components/client-form";import{requireWorkspace}from"@/lib/auth-context";import{updateClientAction}from"../../actions";
export default async function EditClient({params,searchParams}){const{id}=await params;const query=await searchParams;const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("clients").select("*").eq("id",id).eq("workspace_id",workspaceId).single();if(error?.code==="PGRST116")notFound();if(error)throw error;return <AppShell><div className="content narrow"><Link className="back-link" href={`/app/clientes/${id}`}>← {data.name}</Link><div className="eyebrow">Editar cadastro</div><h1 className="page-title">{data.name}</h1><ClientForm client={data} action={updateClientAction.bind(null,id)} submitLabel="Salvar alterações" error={query.erro}/></div></AppShell>}

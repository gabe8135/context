import Link from"next/link";import{AppShell}from"@/components/app-shell";import{TaskForm}from"@/components/task-form";import{requireWorkspace}from"@/lib/auth-context";import{createTaskAction}from"../actions";
export default async function NewTask({searchParams}){const q=await searchParams;const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("projects").select("id,name,slug,clients(name)").eq("workspace_id",workspaceId).in("status",["planned","active","waiting"]).is("archived_at",null).order("name");if(error)throw error;return <AppShell><div className="content narrow"><Link className="back-link" href={q.projeto?`/app/projetos/${q.projeto}`:"/app/tarefas"}>← Voltar</Link><div className="eyebrow">Próxima ação</div><h1 className="page-title">Criar tarefa</h1>{data.length?<TaskForm projects={data} selectedSlug={q.projeto} error={q.erro} action={createTaskAction}/>:<section className="empty-state"><h2>Crie um projeto primeiro</h2><Link className="btn primary" href="/app/projetos/novo">Novo projeto</Link></section>}</div></AppShell>}

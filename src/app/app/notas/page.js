import Link from"next/link";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";export const dynamic="force-dynamic";
export default async function Notes(){const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("notes").select("id,title,content,status,projects(name,slug)").eq("workspace_id",workspaceId).is("archived_at",null).order("updated_at",{ascending:false});if(error)throw error;return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Conhecimento</div><h1 className="page-title">Notas</h1></div><Link className="btn primary" href="/app/notas/nova">Nova nota</Link></div><section className="panel data-panel"><table><thead><tr><th>Nota</th><th>Projeto</th><th>Estado</th></tr></thead><tbody>{data.map(n=><tr key={n.id}><td><Link href={`/app/notas/${n.id}`}><b>{n.title}</b></Link><div className="meta">{n.content.slice(0,120)}</div></td><td><Link href={`/app/projetos/${n.projects?.slug}`}>{n.projects?.name}</Link></td><td><span className="badge">{n.status}</span></td></tr>)}</tbody></table></section></div></AppShell>}

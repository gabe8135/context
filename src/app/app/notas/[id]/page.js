import Link from"next/link";import{notFound}from"next/navigation";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";import{updateNoteAction,archiveNoteAction,restoreNoteVersionAction}from"../actions";
export const dynamic="force-dynamic";export default async function NoteDetail({params,searchParams}){const{id}=await params,q=await searchParams,{supabase,workspaceId}=await requireWorkspace();const[{data:n,error},{data:versions}]=await Promise.all([supabase.from("notes").select("*").eq("id",id).eq("workspace_id",workspaceId).single(),supabase.from("note_versions").select("id,version,title,content,created_at").eq("note_id",id).eq("workspace_id",workspaceId).order("version",{ascending:false})]);if(error?.code==="PGRST116")notFound();if(error)throw error;return <AppShell><div className="content narrow"><Link className="back-link" href="/app/notas">← Notas</Link><div className="eyebrow">Memória versionada · versão {n.version}</div><h1 className="page-title">Editar nota</h1>{q.sucesso&&<p className="success-note">{q.sucesso}</p>}<form action={updateNoteAction.bind(null,id)} className="panel form-panel"><div className="form-grid"><label className="field"><span>Estado</span><select name="status" defaultValue={n.status}><option value="active">Atual</option><option value="historical">Histórica</option></select></label><label className="field full"><span>Título</span><input name="title" defaultValue={n.title} required/></label><label className="field full"><span>Conteúdo</span><textarea name="content" rows="15" defaultValue={n.content} required/><small>Aceita Markdown: títulos, listas, links, negrito e checklists.</small></label></div><button className="btn primary">Salvar nova versão</button></form>{versions?.length>0&&<section className="panel" style={{marginTop:16}}><header className="panel-head"><div className="panel-title">Histórico de versões</div><span className="badge">{versions.length}</span></header>{versions.map(v=><div className="item" key={v.id}><div className="item-main"><b>Versão {v.version} · {v.title}</b><div className="meta">{new Date(v.created_at).toLocaleString("pt-BR")} · {v.content.slice(0,100)}</div></div><form action={restoreNoteVersionAction.bind(null,id,v.id)}><button className="btn">Restaurar</button></form></div>)}</section>}<form action={archiveNoteAction.bind(null,id)}><button className="btn" style={{marginTop:12}}>Arquivar nota</button></form></div></AppShell>}

import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";export const dynamic="force-dynamic";export default async function Decisions(){const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("decisions").select("id,title,content,status,decided_at,responsible_name,projects(name,slug),clients(name)").eq("workspace_id",workspaceId).is("archived_at",null).order("decided_at",{ascending:false});if(error)throw error;return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Memória preservada</div><h1 className="page-title">Decisões</h1><p className="subtitle">O que foi decidido, por quem e o que continua vigente.</p></div><Link className="btn primary" href="/app/decisoes/nova"><Plus size={15}/> Nova decisão</Link></div><section className="panel data-panel" style={{marginTop:28}}><table><thead><tr><th>Decisão</th><th>Projeto</th><th>Status</th><th>Data</th></tr></thead><tbody>{data.map(d=><tr key={d.id}><td><b>{d.title}</b><div className="meta">{d.content.slice(0,100)}</div></td><td><Link href={`/app/projetos/${d.projects?.slug}`}>{d.projects?.name}</Link><div className="meta">{d.clients?.name}</div></td><td><span className="badge">{d.status}</span></td><td>{new Date(d.decided_at).toLocaleDateString("pt-BR")}</td></tr>)}</tbody></table></section></div></AppShell>}

import Link from'next/link';import{notFound}from'next/navigation';import{AppShell}from'@/components/app-shell';import{requireWorkspace}from'@/lib/auth-context';import{archiveDecisionAction,updateDecisionAction}from'./actions';
const dt=v=>v?new Date(v).toISOString().slice(0,16):'';export default async function EditDecision({params}){const{id}=await params,{supabase,workspaceId}=await requireWorkspace();const{data:d,error}=await supabase.from('decisions').select('*,projects(name,slug)').eq('id',id).eq('workspace_id',workspaceId).is('archived_at',null).single();if(error?.code==='PGRST116')notFound();if(error)throw error;return <AppShell><div className="content narrow"><Link className="back-link" href={`/app/projetos/${d.projects?.slug}`}>← {d.projects?.name}</Link><div className="eyebrow">Memória decisória</div><h1 className="page-title">Editar decisão</h1><form className="panel form-panel" action={updateDecisionAction.bind(null,id)}><div className="form-grid"><label className="field full"><span>Título</span><input name="title" defaultValue={d.title} required/></label><label className="field full"><span>Decisão</span><textarea name="content" rows="7" defaultValue={d.content} required/></label><label className="field"><span>Responsável</span><input name="responsible_name" defaultValue={d.responsible_name||''}/></label><label className="field"><span>Data</span><input type="datetime-local" name="decided_at" defaultValue={dt(d.decided_at)}/></label><label className="field"><span>Status</span><select name="status" defaultValue={d.status}><option value="current">Atual</option><option value="pending_confirmation">Pendente</option><option value="superseded">Substituída</option><option value="cancelled">Cancelada</option></select></label><label className="field"><span>Motivo</span><textarea name="reason" rows="4" defaultValue={d.reason||''}/></label><label className="field"><span>Impacto</span><textarea name="impact" rows="4" defaultValue={d.impact||''}/></label></div><button className="btn primary">Salvar alterações</button></form><form action={archiveDecisionAction.bind(null,id)}><button className="btn" style={{marginTop:12}}>Arquivar decisão</button></form></div></AppShell>}

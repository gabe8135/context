import Link from"next/link";import{FolderKanban,Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";
export const dynamic="force-dynamic";export default async function Projects({searchParams}){const q=await searchParams;const{supabase,workspaceId}=await requireWorkspace();const{data,error}=await supabase.from("projects").select("id,name,slug,status,priority,progress,due_at,clients(name)").eq("workspace_id",workspaceId).is("archived_at",null).order("updated_at",{ascending:false});if(error)throw error;return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Operação</div><h1 className="page-title">Projetos</h1><p className="subtitle">Trabalho ativo, contexto e próximos passos por cliente.</p></div><Link className="btn primary" href="/app/projetos/novo"><Plus size={15}/> Novo projeto</Link></div>{q.sucesso&&<p className="success-note">{q.sucesso}</p>}{!data.length?<section className="empty-state"><FolderKanban size={34}/><h2>Nenhum projeto ainda</h2><p>Crie um projeto vinculado ao cliente que você acabou de cadastrar.</p><Link className="btn primary" href="/app/projetos/novo">Criar primeiro projeto</Link></section>:<section className="panel data-panel"><table><thead><tr><th>Projeto</th><th>Cliente</th><th>Progresso</th><th>Prazo</th><th></th></tr></thead><tbody>{data.map(p=><tr key={p.id}><td><b>{p.name}</b><div className="meta">{p.status} · {p.priority}</div></td><td>{p.clients?.name}</td><td><div className="progress" style={{width:110}}><i style={{width:`${p.progress}%`}}/></div><div className="meta">{p.progress}%</div></td><td>{p.due_at?new Date(`${p.due_at}T12:00:00`).toLocaleDateString("pt-BR"):"—"}</td><td><Link className="btn" href={`/app/projetos/${p.slug}`}>Abrir</Link></td></tr>)}</tbody></table></section>}</div></AppShell>}

import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";import{cancelFinancialEntryAction}from"./actions";export const dynamic="force-dynamic";const money=c=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100);
export default async function Finance({searchParams}){const q=await searchParams;const{supabase,workspaceId}=await requireWorkspace();let query=supabase.from("financial_entries").select("id,description,entry_type,status,amount_cents,occurred_at,due_at,projects(name,slug),clients(name)").eq("workspace_id",workspaceId).is("archived_at",null).order("created_at",{ascending:false});if(q.tipo)query=query.eq("entry_type",q.tipo);if(q.status)query=query.eq("status",q.status);const{data,error}=await query;if(error)throw error;const active=data.filter(x=>x.status!=="cancelled"),sum=fn=>active.filter(fn).reduce((s,x)=>s+x.amount_cents,0),received=sum(x=>x.entry_type==="income"&&x.status==="paid"),costs=sum(x=>["expense","tax","service_cost","refund"].includes(x.entry_type)&&x.status==="paid"),discounts=sum(x=>x.entry_type==="discount");return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Controle financeiro</div><h1 className="page-title">Financeiro</h1></div><Link className="btn primary" href="/app/financeiro/novo"><Plus size={15}/> Novo lançamento</Link></div><section className="metrics"><Metric l="Recebido" v={money(received)}/><Metric l="Custos pagos" v={money(costs)}/><Metric l="Descontos" v={money(discounts)}/><Metric l="Resultado" v={money(received-costs)}/></section><form className="filter-bar"><select name="tipo" defaultValue={q.tipo||""}><option value="">Todos os tipos</option><option value="income">Receita</option><option value="expense">Despesa</option><option value="discount">Desconto</option><option value="tax">Imposto</option><option value="service_cost">Custo de serviço</option></select><select name="status" defaultValue={q.status||""}><option value="">Todos os status</option><option value="pending">Pendente</option><option value="paid">Pago</option><option value="overdue">Vencido</option><option value="cancelled">Cancelado</option></select><button className="btn">Filtrar</button><Link className="btn" href="/app/financeiro">Limpar</Link></form><section className="panel data-panel"><table><thead><tr><th>Descrição</th><th>Projeto</th><th>Status</th><th>Valor</th><th></th></tr></thead><tbody>{data.map(e=><tr key={e.id} style={{opacity:e.status==="cancelled"?.55:1}}><td><b>{e.description}</b><div className="meta">{e.entry_type}</div></td><td><Link href={`/app/projetos/${e.projects?.slug}`}>{e.projects?.name}</Link></td><td><span className="badge">{e.status}</span></td><td className="mono">{money(e.amount_cents)}</td><td>{e.status!=="cancelled"&&<form action={cancelFinancialEntryAction.bind(null,e.id)}><button className="btn">Cancelar</button></form>}</td></tr>)}</tbody></table>{!data.length&&<div className="empty">Nenhum lançamento neste filtro.</div>}</section></div></AppShell>}
function Metric({l,v}){return <div className="metric"><div className="metric-label">{l}</div><div className="metric-value mono">{v}</div></div>}

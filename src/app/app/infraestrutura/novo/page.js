import Link from"next/link";import{AppShell}from"@/components/app-shell";const items=[["hosting","Hospedagem"],["dns","Registro DNS"],["ssl","Certificado SSL"],["email","Serviço de e-mail"],["integration","Integração"]];export default function Hub(){return <AppShell><div className="content"><div className="eyebrow">Infraestrutura</div><h1 className="page-title">Adicionar item técnico</h1><div className="metrics">{items.map(([id,label])=><Link className="metric" style={{textDecoration:"none",color:"inherit"}} href={`/app/infraestrutura/novo/${id}`} key={id}><div className="metric-label">Novo registro</div><div className="panel-title" style={{marginTop:8}}>{label}</div></Link>)}</div></div></AppShell>}

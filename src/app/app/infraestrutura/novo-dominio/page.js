import Link from"next/link";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";import{createDomainAction}from"../actions";export default async function NewDomain({searchParams}){const q=await searchParams;const{supabase,workspaceId}=await requireWorkspace();const{data}=await supabase.from("projects").select("id,name").eq("workspace_id",workspaceId).is("archived_at",null).order("name");return <AppShell><div className="content narrow"><Link className="back-link" href="/app/infraestrutura">← Infraestrutura</Link><div className="eyebrow">Ativo técnico</div><h1 className="page-title">Adicionar domínio</h1><form action={createDomainAction} className="panel form-panel">{q.erro&&<p className="error">{q.erro}</p>}<div className="form-grid"><label className="field"><span>Projeto</span><select name="project_id" required>{data?.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label className="field"><span>Domínio</span><input name="domain" placeholder="exemplo.com.br" required/></label><label className="field"><span>Registrador</span><input name="registrar"/></label><label className="field"><span>Titular</span><input name="holder"/></label><label className="field"><span>DNS atual</span><input name="current_dns"/></label><label className="field"><span>Status</span><select name="status"><option value="operational">Operacional</option><option value="attention">Atenção</option><option value="pending">Pendente</option><option value="error">Erro</option><option value="unverified">Não verificado</option></select></label><label className="field"><span>Registro</span><input type="date" name="registered_at"/></label><label className="field"><span>Vencimento</span><input type="date" name="expires_at"/></label><label className="field"><span><input type="checkbox" name="auto_renew"/> Renovação automática</span></label><label className="field full"><span>Observações</span><textarea name="notes" rows="4"/></label></div><div className="form-actions"><button className="btn primary">Salvar domínio</button></div></form></div></AppShell>}

"use server";import{revalidatePath}from'next/cache';import{requireWorkspace}from'@/lib/auth-context';import{infrastructureModules}from'@/lib/infrastructure-modules';export async function restoreInfrastructureAction(type,id){const cfg=infrastructureModules[type];if(!cfg)throw new Error('Tipo inválido');const{supabase,workspaceId}=await requireWorkspace();const{error}=await supabase.from(cfg.table).update({archived_at:null}).eq('id',id).eq('workspace_id',workspaceId);if(error)throw error;revalidatePath('/app/infraestrutura/arquivados');revalidatePath('/app/infraestrutura')}

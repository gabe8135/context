import Link from "next/link";import{notFound}from"next/navigation";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{requireWorkspace}from"@/lib/auth-context";import{operationalModules}from"@/lib/operational-modules";import{archiveOperationalAction,classifyInboxAction}from"../actions";
export const dynamic="force-dynamic";
export default async function OperationalList({params,searchParams}){const{tipo}=await params,q=await searchParams,cfg=operationalModules[tipo];if(!cfg)notFound();const{supabase,workspaceId}=await requireWorkspace();let projectId=null;if(q.projeto){const{data}=await supabase.from("projects").select("id").eq("slug",q.projeto).eq("workspace_id",workspaceId).maybeSingle();projectId=data?.id||null}let query=supabase.from(cfg.table).select(cfg.select).eq("workspace_id",workspaceId).is("archived_at",null).order("created_at",{ascending:false}).limit(100);if(projectId)query=query.eq("project_id",projectId);const{data,error}=await query;if(error)throw error;return <AppShell><div className="content"><div className="project-head"><div><div className="eyebrow">Contexto estruturado</div><h1 className="page-title">{cfg.title}</h1>{q.projeto&&<p className="subtitle">Projeto: {q.projeto.replaceAll("-"," ")}</p>}</div><Link className="btn primary" href={`/app/operacao/${tipo}/novo${q.projeto?`?projeto=${q.projeto}`:""}`}><Plus size={15}/> Adicionar {cfg.singular}</Link></div>{q.sucesso&&<p className="success-note">{q.sucesso}</p>}{q.erro&&<p className="error">{q.erro}</p>}<section className="panel" style={{marginTop:24}}>{data.length?data.map(x=><div className="item" key={x.id}><div className="item-main"><div className="item-title">{x[cfg.name]}</div><div className="meta">{x.projects?.name||"Sem projeto"} · {x.status||x.category||`versão ${x.version||1}`}</div></div><div className="actions" style={{margin:0}}>{tipo!=="entrada"&&<Link className="btn" href={`/app/operacao/${tipo}/${x.id}/editar`}>Editar</Link>}{tipo==="entrada"&&x.status==="unclassified"&&<><form action={classifyInboxAction.bind(null,x.id,"task")}><button className="btn">Virar tarefa</button></form><form action={classifyInboxAction.bind(null,x.id,"note")}><button className="btn">Virar nota</button></form></>}{x.access_url&&<a className="btn" href={x.access_url} target="_blank" rel="noreferrer">Abrir</a>}<form action={archiveOperationalAction.bind(null,tipo,x.id)}><button className="btn">Arquivar</button></form></div></div>):<div className="empty">Nenhum registro. Crie o primeiro item.</div>}</section></div></AppShell>}

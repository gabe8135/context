import Link from'next/link';import{AppShell}from'@/components/app-shell';import{requireWorkspace}from'@/lib/auth-context';import{uploadFileAction}from'../actions';
export default async function NewFile({searchParams}){const q=await searchParams,{supabase,workspaceId}=await requireWorkspace();const{data}=await supabase.from('projects').select('id,name').eq('workspace_id',workspaceId).is('archived_at',null).order('name');return <AppShell><div className="content narrow"><Link className="back-link" href="/app/operacao/arquivos">← Arquivos</Link><div className="eyebrow">Storage privado</div><h1 className="page-title">Enviar arquivo</h1><p className="subtitle">Até 25 MB. O nome lógico permanece; novas cargas criam versões.</p><form action={uploadFileAction} className="panel form-panel">{q.erro&&<p className="error">{q.erro}</p>}<div className="form-grid"><label className="field"><span>Projeto</span><select name="project_id" required><option value="">Selecione</option>{data?.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label className="field"><span>Categoria</span><input name="category" placeholder="contrato, logo, backup..."/></label><label className="field full"><span>Nome lógico</span><input name="logical_name" required placeholder="Logo principal"/></label><label className="field full"><span>Arquivo</span><input name="file" type="file" required/></label><label className="field full"><span>Descrição</span><textarea name="description" rows="4"/></label></div><div className="form-actions"><button className="btn primary">Enviar arquivo</button></div></form></div></AppShell>}

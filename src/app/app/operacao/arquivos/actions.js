"use server";import{redirect}from"next/navigation";import{requireWorkspace}from"@/lib/auth-context";const MAX=25*1024*1024,allowed=new Set(['application/pdf','image/png','image/jpeg','image/webp','video/mp4','application/zip','application/x-zip-compressed','text/plain','text/csv','application/json','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet']);
export async function uploadFileAction(fd){const{supabase,user,workspaceId}=await requireWorkspace();const file=fd.get('file'),projectId=String(fd.get('project_id')||''),logical=String(fd.get('logical_name')||'').trim();if(!(file instanceof File)||!file.size)redirect('/app/operacao/arquivos/novo?erro=Selecione um arquivo');if(file.size>MAX)redirect('/app/operacao/arquivos/novo?erro=O limite é 25 MB');if(!allowed.has(file.type))redirect('/app/operacao/arquivos/novo?erro=Tipo não permitido');const{data:p,error:pError}=await supabase.from('projects').select('client_id').eq('id',projectId).eq('workspace_id',workspaceId).single();if(pError)throw pError;const{data:last}=await supabase.from('files').select('version').eq('workspace_id',workspaceId).eq('project_id',projectId).eq('logical_name',logical).order('version',{ascending:false}).limit(1).maybeSingle();const version=(last?.version||0)+1,safe=file.name.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9._-]/g,'-'),path=`${workspaceId}/${projectId}/${crypto.randomUUID()}-${safe}`;const{error:uploadError}=await supabase.storage.from('workspace-files').upload(path,file,{contentType:file.type,upsert:false});if(uploadError)throw uploadError;await supabase.from('files').update({is_current:false}).eq('workspace_id',workspaceId).eq('project_id',projectId).eq('logical_name',logical);const{error}=await supabase.from('files').insert({workspace_id:workspaceId,project_id:projectId,client_id:p.client_id,logical_name:logical,category:String(fd.get('category')||''),storage_path:path,mime_type:file.type,size_bytes:file.size,version,description:String(fd.get('description')||''),is_current:true,created_by:user.id});if(error){await supabase.storage.from('workspace-files').remove([path]);throw error}redirect(`/app/operacao/arquivos?sucesso=Arquivo enviado na versão ${version}`)}
export async function updateFileAction(id,fd){const{supabase,workspaceId}=await requireWorkspace();const{error}=await supabase.from('files').update({logical_name:String(fd.get('logical_name')||'').trim(),category:String(fd.get('category')||'').trim(),description:String(fd.get('description')||'').trim()}).eq('id',id).eq('workspace_id',workspaceId);if(error)throw error;redirect('/app/operacao/arquivos?sucesso=Arquivo atualizado')}
export async function setCurrentFileAction(id){const{supabase,workspaceId}=await requireWorkspace();const{data:f,error}=await supabase.from('files').select('project_id,logical_name').eq('id',id).eq('workspace_id',workspaceId).single();if(error)throw error;await supabase.from('files').update({is_current:false}).eq('workspace_id',workspaceId).eq('project_id',f.project_id).eq('logical_name',f.logical_name);const{error:e}=await supabase.from('files').update({is_current:true}).eq('id',id).eq('workspace_id',workspaceId);if(e)throw e;redirect('/app/operacao/arquivos?sucesso=Versão atual alterada')}
export async function archiveFileAction(id){const{supabase,workspaceId}=await requireWorkspace();const{error}=await supabase.from('files').update({archived_at:new Date().toISOString(),is_current:false}).eq('id',id).eq('workspace_id',workspaceId);if(error)throw error;redirect('/app/operacao/arquivos?sucesso=Arquivo arquivado')}
export async function restoreFileAction(id){const{supabase,workspaceId}=await requireWorkspace();const{error}=await supabase.from('files').update({archived_at:null}).eq('id',id).eq('workspace_id',workspaceId);if(error)throw error;redirect('/app/operacao/arquivos?sucesso=Arquivo restaurado')}

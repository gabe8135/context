"use server";import{headers}from"next/headers";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export async function requestReset(fd){const email=String(fd.get("email")||"").trim().toLowerCase();const origin=(await headers()).get("origin")||"http://localhost:3000";const supabase=await createClient();const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${origin}/auth/callback?next=/atualizar-senha`});if(error)redirect(`/recuperar-senha?erro=${encodeURIComponent(error.message)}`);redirect("/recuperar-senha?mensagem=Confira seu e-mail para redefinir a senha")}

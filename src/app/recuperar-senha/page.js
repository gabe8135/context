import Link from"next/link";import{requestReset}from"./actions";export default async function Recover({searchParams}){const q=await searchParams;return <main className="login-card"><div className="eyebrow">Recuperar acesso</div><h1>Redefina sua senha</h1>{q.erro&&<p className="error">{q.erro}</p>}{q.mensagem&&<p className="success-note">{q.mensagem}</p>}<form action={requestReset}><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><button className="btn primary">Enviar link</button></form><p><Link href="/login">Voltar ao login</Link></p></main>}

import{createClient}from"@supabase/supabase-js";let client;export function getAdminClient(){if(!client){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY;if(!url||!key)throw new Error("SUPABASE_SECRET_KEY não configurada");client=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}return client}

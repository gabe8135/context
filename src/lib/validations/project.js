import { z } from "zod";
export const projectSchema=z.object({client_id:z.string().uuid("Selecione um cliente."),name:z.string().trim().min(2,"Informe o nome do projeto.").max(140),description:z.string().trim().max(3000).optional(),status:z.enum(["planned","active","waiting","completed","cancelled","archived"]),priority:z.enum(["low","medium","high","urgent"]),starts_at:z.string().optional(),due_at:z.string().optional(),agreed_value:z.string().optional(),next_action:z.string().trim().max(300).optional()});
export function projectPayload(formData){const value=projectSchema.parse(Object.fromEntries(formData.entries()));const number=Number(String(value.agreed_value||"0").replace(".","").replace(",","."));return{...value,starts_at:value.starts_at||null,due_at:value.due_at||null,agreed_value_cents:Number.isFinite(number)?Math.round(number*100):0,agreed_value:undefined}}
export function slugify(text){return text.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"").slice(0,70)||"projeto"}

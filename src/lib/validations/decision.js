import{z}from"zod";export const decisionSchema=z.object({project_id:z.string().uuid(),title:z.string().trim().min(2,"Informe o título."),content:z.string().trim().min(5,"Descreva a decisão."),reason:z.string().trim().max(2000).optional(),impact:z.string().trim().max(2000).optional(),responsible_name:z.string().trim().max(120).optional(),status:z.enum(["current","cancelled","pending_confirmation"]),decided_at:z.string().optional(),supersedes_id:z.string().optional()});export function decisionPayload(fd){const v=decisionSchema.parse(Object.fromEntries(fd.entries()));return{...v,decided_at:v.decided_at?new Date(v.decided_at).toISOString():new Date().toISOString(),supersedes_id:undefined}}
